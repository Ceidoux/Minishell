	char	buff[BUFFER_SIZE];
	int		i;

	i = 0;
	read(0, buff, BUFFER_SIZE);
	while (buff[i] != '\n')
	{
		write(1, &buff[i++], 1);
		if (i == BUFFER_SIZE)
		{
			i = 0;
			read(0, buff, BUFFER_SIZE);
		}
	}
